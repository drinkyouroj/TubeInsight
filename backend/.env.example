# Flask specific
FLASK_APP=app.py # Or main.py, depending on your entry point script name
FLASK_ENV=development # set to 'production' in production
FLASK_DEBUG=True # set to False in production
FLASK_SECRET_KEY=your_very_strong_and_secret_flask_key_here # CHANGE THIS! Generate a random string.

# Supabase Credentials
# Find these in your Supabase project settings under API
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_KEY=your_supabase_service_role_key # This is a secret key, keep it safe!
SUPABASE_JWT_SECRET=your_supabase_jwt_secret # This is a secret key, keep it safe!

# OpenAI API Key
# Find this in your OpenAI dashboard under API keys
OPENAI_API_KEY=your_openai_api_key_here

# YouTube Data API v3 Key
# Find this in your Google Cloud Console project under APIs & Services > Credentials
YOUTUBE_API_KEY=your_youtube_data_api_key_here

# CORS Origins (Optional - for production, restrict this)
# Example: FRONTEND_URL=http://localhost:3000
# If you configure CORS in __init__.py to use an env var for allowed origins.
# For now, __init__.py uses "*", so this is not strictly needed yet.
# ALLOWED_ORIGINS=http://localhost:3000,https://your-production-frontend.com
